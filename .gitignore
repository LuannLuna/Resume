*DS_Store

# LaTeX auxiliary files (safety net for root directory)
*.aux
*.log
*.out
*.toc
*.synctex.gz
*.fdb_latexmk
*.fls

# Ensure PDFs are tracked (negation pattern)
!*.pdf

# Ignore V2 Portuguese resume (local only, never commit to server)
Luann_Luna_Resume_V2_PT.pdf