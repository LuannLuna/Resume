*DS_Store

# Local documents and specs
docs/

# LaTeX auxiliary files (safety net for root directory)
*.aux
*.log
*.out
*.toc
*.synctex.gz
*.fdb_latexmk
*.fls

# Ensure PDFs are tracked (negation pattern)
!*.pdf

# Local tool configuration
.claude/
